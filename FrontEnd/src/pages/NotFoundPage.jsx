import React from "react";
import { NotFound, Container } from "../components/index";

const NotFoundPage = () => {
  return (
    <Container>
      <NotFound />
    </Container>
  );
};

export default NotFoundPage;